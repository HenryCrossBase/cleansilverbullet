const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const dep = await prisma.cryptoDeposit.findFirst({ where: { trackId: '110050344', status: 'PENDING' }}); if (!dep) { console.log('Already processed'); return; } await prisma.$transaction(async (tx) => { await tx.cryptoDeposit.update({ where: { id: dep.id }, data: { status: 'COMPLETED' }}); await tx.user.update({ where: { id: dep.userId }, data: { credits: { increment: dep.bulletsReceived }}}); console.log('Force-credited AlirezaCR!'); }); } main().finally(() => process.exit(0));
